import styled from "styled-components";

export const AdminPanelC = styled.main`
    height: 100%;
    position: relative;
    display: grid;
    grid-template-columns: 300px auto;
    .outline-panel{
        padding-right: 1rem;
        padding-top: 1rem;
    }
`